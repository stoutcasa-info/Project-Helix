Project AWS Helix: Terraform-Powered Database Migration Simulator

![Status](https://img.shields.io/badge/Status-Active-brightgreen)
![Duration](https://img.shields.io/badge/Duration-30%20Days-blue)
![Level](https://img.shields.io/badge/Level-Intermediate-orange)
![Platform](https://img.shields.io/badge/Platform-AWS%20Free%20Tier-informational)
![Tools](https://img.shields.io/badge/Tools-Python%20%7C%20Terraform%20%7C%20RDS%20%7C%20S3-informational)

## Executive Summary

**Project AWS Helix** is a self-paced, 30-day hands-on training module that mirrors the original Project Helix, but deployed on AWS. You will migrate records from a legacy, non-normalized flat-file / legacy MySQL schema (the **Source**) to a modern normalized relational schema on Amazon RDS (the **Target**), automating the entire pipeline with Python and Terraform.

> **Estimated Time:** ~1.5 hours/day  
> **Theme:** Data Migration, Normalization, and Infrastructure as Code (IaC) on AWS using Terraform

---

## Table of Contents

- [Technical Architecture](#technical-architecture)
- [Prerequisites](#prerequisites)
- [Project Structure](#project-structure)
- [Quick Start](#quick-start)
- [Module Breakdown](#module-breakdown)
  - [Week 1 — Foundations](#week-1--foundations)
  - [Week 2 — The Migration](#week-2--the-migration)
  - [Week 3 — Automation](#week-3--automation)
  - [Week 4 — Verification & Documentation](#week-4--verification--documentation)
- [Schema Design](#schema-design)
- [Key Engineering Concepts](#key-engineering-concepts)
- [Verification](#verification)
- [30-Day Calendar](#30-day-calendar)
- [Contributing](#contributing)
- [License](#license)

---

## Technical Architecture

| Layer          | Technology / Service           | Purpose                                                     |
|----------------|--------------------------------|-------------------------------------------------------------|
| Infrastructure | Terraform + AWS                | Provision VPC, S3, and RDS with IaC                        |
| Storage        | Amazon S3                      | Store legacy flat-file CSV input                           |
| Database       | Amazon RDS for MySQL           | Legacy (`legacy`) and modern (`modern`) schemas            |
| ETL Engine     | Python (Pandas, SQLAlchemy)    | Extract, transform, and load between schemas               |
| Automation     | Terraform + shell/PowerShell   | Reproducible environment and pipeline execution            |
| Design         | draw.io / diagrams.net         | ER diagrams (Crow’s Foot notation)                         |

---

## Prerequisites

Before starting **Day 1**, ensure you have:

- An AWS Free Tier account with billing alarms configured.
- Terraform installed (v1.3+ recommended).
- AWS CLI installed and configured (`aws configure`).
- Python 3.10+ with `pip`.
- MySQL client (MySQL Workbench, DBeaver, or CLI).
- VS Code or your preferred editor.

### Install Python Dependencies

From the project root:

```bash
pip install pandas sqlalchemy mysql-connector-python boto3 faker
Project Structure
bash
Project_AWS_Helix/
├── infrastructure/
│   └── main.tf               # Terraform IaC — VPC, subnets, security groups, S3, RDS
├── scripts/
│   ├── generate_data.py      # Generates synthetic legacy flat-file data (CSV + S3)
│   ├── migration_engine.py   # Core ETL pipeline (Extract → Transform → Load)
│   └── verify_migration.py   # Post-migration data quality checks
├── data/
│   └── legacy_source_data.csv  # Auto-generated by generate_data.py
├── docs/
│   └── Architecture_AWS_Migration.png  # ER diagrams (Crow's Foot notation)
└── README.md
Quick Start
Complete these steps to run the full AWS migration pipeline end-to-end.

1. Provision Infrastructure with Terraform
From the infrastructure directory:

bash
terraform init
terraform apply -auto-approve
This provisions:

VPC, subnets, and security groups

Amazon S3 bucket for legacy CSV

Amazon RDS for MySQL with legacy and modern schemas (or one DB where you’ll create both schemas)

Terraform outputs will include:

RDS endpoint

S3 bucket name

DB username/password (or reference to AWS Secrets Manager, depending on your setup)

2. Generate Legacy Data
From the project root:

bash
python scripts/generate_data.py
This script:

Creates data/legacy_source_data.csv with non-normalized records (similar to a mainframe export).

Uploads the CSV to the Terraform-created S3 bucket (if configured).

3. Load Data into Legacy DB
Use either:

Option A — Python loader (recommended)
Extend generate_data.py or add a helper to:

Read legacy_source_data.csv.

Connect to RDS using SQLAlchemy.

Create legacy.RECORD_RAW if it doesn’t exist.

Bulk insert the CSV rows.

Option B — MySQL client

Download or copy legacy_source_data.csv to the client machine that can connect to the RDS instance.

Run SQL similar to:

sql
CREATE DATABASE IF NOT EXISTS legacy;
USE legacy;

CREATE TABLE IF NOT EXISTS RECORD_RAW (
    raw_id          INT AUTO_INCREMENT PRIMARY KEY,
    full_name       VARCHAR(255),
    dob_string      VARCHAR(50),
    category_string TEXT,
    last_event_date VARCHAR(50)
);

LOAD DATA LOCAL INFILE 'legacy_source_data.csv'
INTO TABLE RECORD_RAW
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
(full_name, dob_string, category_string, last_event_date);
4. Run the Migration
bash
python scripts/migration_engine.py
This script:

Reads from legacy.RECORD_RAW on RDS.

Transforms records into normalized form.

Loads into modern.ENTITY_MASTER and modern.EVENT_LOG.

Module Breakdown
Week 1 — Foundations
Day 1: Install tools; create Project_AWS_Helix folder structure.

Day 2: Design ER diagrams (legacy vs modern) and save to /docs.

Day 3: Author main.tf for provider, VPC, and S3.

Day 4: Add RDS instance and necessary networking to main.tf.

Day 5: Implement and run generate_data.py; verify CSV locally and in S3.

Weekend: Test RDS connectivity; explore data using MySQL Workbench.

Week 2 — The Migration
Day 8: Create legacy.RECORD_RAW in RDS and load CSV.

Day 9: Implement migration_engine.py “Connect & Extract” logic.

Day 10: Implement “Transform” (name parsing, date normalization, category explosion).

Day 11: Implement “Load” into modern.ENTITY_MASTER.

Day 12: Implement “Load” into modern.EVENT_LOG, mapping LegacyID to EntityID.

Weekend: Run end-to-end migration, validate in the modern schema.

Week 3 — Automation
Day 15: Parameterize Terraform (variables for DB sizes, names, etc.).

Day 16: Create a deployment helper script to run Terraform and scripts in order.

Day 17: Add environment configuration (config file or env vars) for scripts.

Day 18: “Nuke and Pave” — terraform destroy, then terraform apply and full rerun.

Days 19–21: Refine Terraform layout, add comments, and improve logging in scripts.

Week 4 — Verification & Documentation
Day 22: Implement verify_migration.py with row-count comparisons and sample checks.

Day 23: Run verification and save logs to /docs.

Day 24: Write and refine this README.md.

Days 25–29: Full review, interview-style explanations, and code polish.

Day 30: ✅ Final clean Nuke & Pave, successful run, push to GitHub.

Schema Design
Legacy Source (Flat File / Legacy Schema)
text
RECORD_RAW
──────────────────────────────
raw_id           INT  (PK)
full_name        VARCHAR(255)   ← "LASTNAME, FIRSTNAME" format
dob_string       VARCHAR(50)    ← Mixed formats: YYYYMMDD or YYYY-MM-DD
category_string  TEXT           ← Anti-pattern: "CAT-A1,CAT-B2,CAT-C3"
last_event_date  VARCHAR(50)
Modern Target (Normalized Relational Schema)
text
ENTITY_MASTER                           EVENT_LOG
──────────────────────────              ──────────────────────────────
EntityID   INT  (PK)  ◄──────────────  EntityID    INT  (FK)
FirstName  VARCHAR                      EventID     INT  (PK)
LastName   VARCHAR                      CategoryCode VARCHAR
DOB        DATE                         EventDate    DATE
LegacyID   INT  (traceability)
One ENTITY_MASTER record → Many EVENT_LOG records (1:N relationship)

Key Engineering Concepts
Concept	Where Applied
Data Normalization (1NF)	Splitting category_string into rows
Infrastructure as Code	Terraform main.tf provisioning AWS resources
ETL Pipeline Design	migration_engine.py (Extract → Transform → Load)
Date Standardization	Pandas date parsing for mixed string formats
Name Parsing	Splitting "LASTNAME, FIRSTNAME"
Foreign Key Resolution	Mapping LegacyID → EntityID
Data Quality Verification	Row counts, sample checks in verify_migration.py
Reproducibility	Terraform “Nuke and Pave” cycle
Verification
After running python scripts/verify_migration.py, you should see something like:

text
Legacy Source Rows        : 2000
Modern ENTITY_MASTER Rows : 2000
Modern EVENT_LOG Rows     : 6000
✅ SUCCESS: Record counts and mappings match expectations.

--- Spot Check: Legacy Record ID 1 ---
Legacy  : JOHNSON, MICHAEL
Modern  : FirstName=MICHAEL, LastName=JOHNSON
Discrepancies suggest issues with transformation or loading. Start debugging with:

Name parsing logic.

Date coercion and invalid rows.

Category splitting and joins to ENTITY_MASTER.

30-Day Calendar
Week	Days	Focus
Week 1	1–5	Environment setup, AWS architecture, data generation
Week 2	8–12	RDS loading, Python ETL (extract, transform, load)
Week 3	15–21	Terraform automation, Nuke & Pave, script refinement
Week 4	22–30	Verification, documentation, interview prep
Contributing
Fork the repository

Create a feature branch (git checkout -b feature/your-improvement)

Commit your changes (git commit -m 'Add improvement')

Push to the branch (git push origin feature/your-improvement)

Open a Pull Request

License
This project is licensed under the MIT License. See the LICENSE file for details.

Built as a hands-on training module for data engineering and cloud migration on AWS, inspired by Project Helix
